'use strict';

/**
 *  discovery-pass controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::discovery-pass.discovery-pass');
